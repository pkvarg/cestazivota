# .github/workflows/deploy.yml
name: Deploy to Hostinger

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ftp # Use the ftp environment

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build project
        env:
          VITE_EMAILJS_SERVICE: ${{ vars.VITE_EMAILJS_SERVICE }}
          VITE_EMAILJS_TEMPLATE: ${{ vars.VITE_EMAILJS_TEMPLATE }}
          VITE_EMAILJS_USER: ${{ vars.VITE_EMAILJS_USER }}
          VITE_EMAIL_EXTRA_ONE: ${{ vars.VITE_EMAIL_EXTRA_ONE }}
          VITE_EMAIL_EXTRA_TWO: ${{ vars.VITE_EMAIL_EXTRA_TWO }}
        run: npm run build

      - name: Debug variables
        run: |
          echo "HOST: ${{ vars.FTP_HOST }}"
          echo "USERNAME: ${{ vars.FTP_USERNAME }}"
          echo "PASSWORD: ${{ vars.FTP_PASSWORD }}"

      - name: Deploy to Hostinger via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ vars.FTP_HOST }}
          username: ${{ vars.FTP_USERNAME }}
          password: ${{ vars.FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: /public_html/
